{
  "name": "tribeca-cafe",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready premium Mediterranean cafe website and admin system for TRIBECA CAFE.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "seed": "npm run seed --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
